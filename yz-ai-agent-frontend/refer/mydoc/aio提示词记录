
优化客户信息页面，优化清单：
1. 客户信息缺少展示联系人2和对应的联系电话、经营范围、备注1、备注2、备注3
2. 加入的备注字段只需在表格中呈现为一列，将备注1、备注2、备注3的信息分别用一个a-label标签纵向自适应展示
3. 增加筛选条件：公司税号、注册地址、注册电话、银行名称、银行账号、联系人、电话，加入后的筛选顺序和字段展示顺序保持一致，，并在搜索后增加重置按钮，如果筛选条件一行放不下自适应换行

优化公司管理页面，优化清单：
1. 备注1字段改成备注字段，将备注1、备注2、备注3的信息分别用一个a-label标签纵向自适应展示
2. 增加筛选条件：公司税号、注册地址、注册电话、银行名称、银行账号，加入后的筛选顺序和字段展示顺序保持一致，并在搜索后增加重置按钮，如果筛选条件一行放不下自适应换行

对于发票管理中导航栏内的发票开票页面优化：
1. 表格中的客户单位字段应当提前到开票日期后面的字段位置
2. 开票单位字段应当提前到客户单位的前面
3. 总金额、到账金额、未到金额、红字发票等统计信息不要重复展示，只需要展示到表格末尾后即可
3. 修复自定义列后点击确定后修改未生效的问题
4. 标题去除查询结果，而你需要做的是沿用原来的flex布局，左侧包含导出、批量导入的按钮，右侧是自定义列和创建发票的功能按钮，样式要求是除了导入按钮之外，其他按钮的用ghost样式
5. 对于非红字发票并且未到账的记录用黄底背景标识，并且增加一个操作字段，对于这些记录，增加入账的按钮，点击后添加一个modal窗口，输入到账金额和到账日期，日期默认选择今天


对于发票到款信息页面的优化：
1. 备注1字段改成备注字段，将备注1、备注2、备注3的信息分别用一个a-label标签纵向自适应展示
2. 在关联开票ID后添加发票号码的字段信息
3. 增加筛选条件：转账方式，用下拉框的方式

对于发票管理整体页面的优化：
1. 删除发票开票信息相关的内容，包括左侧的导航栏、路由信息、页面


作为一名专业的前端工程师，请你优化该项目，优化内容：
#### 一、核心优化目标
针对客户信息、公司管理、发票管理（开票/到款/整体）四类页面进行结构化优化，需严格遵循字段展示、交互逻辑、样式规范及功能修复要求，确保页面交互友好、信息展示完整、操作逻辑闭环。

#### 二、分页面优化细则
##### （一）客户信息页面优化
1. **字段补充展示**：
   - 新增展示字段：联系人2及对应联系电话、经营范围、备注1、备注2、备注3；
   - 备注字段展示规则：表格中仅保留“备注”一列，备注1/2/3的信息需分别通过`<a-label>`标签纵向自适应展示（标签纵向排列，高度随内容自适应）。
2. **筛选功能优化**：
   - 新增筛选条件（按以下顺序展示）：公司税号、注册地址、注册电话、银行名称、银行账号、联系人、电话；
   - 交互规则：筛选条件区域支持自适应换行（一行放不下时自动换行）；搜索按钮后新增“重置”按钮，点击可清空所有筛选条件并重置页面数据。

##### （二）公司管理页面优化
1. **备注字段调整**：
   - 将原“备注1”字段名称改为“备注”；
   - 备注内容展示规则：备注1/2/3的信息分别通过`<a-label>`标签纵向自适应展示（标签纵向排列，高度随内容自适应）。
2. **筛选功能优化**：
   - 新增筛选条件（按以下顺序展示）：公司税号、注册地址、注册电话、银行名称、银行账号；
   - 交互规则：筛选条件区域支持自适应换行（一行放不下时自动换行）；搜索按钮后新增“重置”按钮，点击可清空所有筛选条件并重置页面数据。

##### （三）发票管理-发票开票页面优化
1. **表格字段排序**：
   - 开票单位字段：调整至“开票日期”后、“客户单位”前；
   - 客户单位字段：调整至“开票单位”后。
2. **统计信息展示**：
   - 总金额、到账金额、未到金额、红字发票等统计信息：仅展示在表格末尾，删除其他重复展示位置。
3. **功能BUG修复**：
   - 自定义列功能：修复“点击确定后列配置修改未生效”的问题，确保配置即时生效。
4. **页面标题与按钮样式**：
   - 标题优化：删除标题中的“查询结果”字样；
   - 布局与样式：沿用原有flex布局，左侧放置导出、批量导入按钮，右侧放置自定义列、创建发票按钮；按钮样式要求：除“批量导入”按钮外，其余按钮均使用ghost样式。
5. **表格行标识与操作**：
   - 行背景标识：非红字发票且未到账的记录，添加黄色背景标识；
   - 操作字段：为上述标识记录新增“入账”操作按钮；点击“入账”按钮弹出modal窗口，窗口内包含“到账金额”输入框、“到账日期”选择框（日期默认选中当天），提交后同步更新数据。

##### （四）发票到款信息页面优化
1. **备注字段调整**：
   - 将原“备注1”字段名称改为“备注”；
   - 备注内容展示规则：备注1/2/3的信息分别通过`<a-label>`标签纵向自适应展示（标签纵向排列，高度随内容自适应）。
2. **字段补充**：
   - 新增字段：在“关联开票ID”字段后添加“发票号码”字段，展示对应发票号码信息。
3. **筛选功能优化**：
   - 新增筛选条件：转账方式，筛选控件采用下拉框形式，下拉选项需覆盖所有业务场景的转账类型。

##### （五）发票管理整体页面优化
1. **内容删除**：
   - 彻底删除与“发票开票信息”相关的所有内容，包括：左侧导航栏中的发票开票入口、对应路由配置、发票开票页面文件及相关依赖。

#### 三、通用要求
1. 所有新增/调整的字段需适配页面响应式布局，确保在PC端不同分辨率下展示正常，无样式错乱；
2. 交互逻辑需符合用户操作习惯，按钮点击、筛选、弹窗等操作无卡顿、无报错；
3. 样式规范需与现有系统UI风格保持一致，`<a-label>`标签、ghost按钮等组件使用系统统一样式；
发票查询统计中优化：
1. 解决导出、自定义列按钮在正常情况下未显示的问题
2. 对于发票到款且不为红字发票的记录，添加两个操作：设为红色发票（要求红色标签并且添加确认机制）；取消到账
3. 对于所有记录添加删除和编辑操作
4. 优化展示，查询条件的标题部分，更改为开票单位的下拉框组件，自动查询所有的公司名称，并添加一个所有的记录
5. 优化筛选顺序，开始日期和结束日期放在客户联系人后面的位置
6. 实现发票添加功能，点击后创建发票添加弹窗，填入必要的字段信息，其中开票日期默认今天，开票单位自动填入下拉框的公司名称，到账日期和到账金额记录不能在里面填入，发票性质是下拉框

客户管理
1. 不要合并联系人2，请将其改为联系人2，电话字段，添加自定义列的功能
2. 优化创建客户样式

公司管理优化创建公司样式

发票查询统计中优化：
1. 开票单位查询选择后，立即执行筛选操作
2. 编辑放入到作废前面，优化操作展示，放在一列中堆叠自适应，优化备注展示，3个备注放在一列中自适应标签展示
3. 红字发票的红色和未入账的黄色背景优化，整条记录都要标上颜色
4. mock的数据修改为我给你的excel表格，对应的是客户信息、公司信息、以及发票信息

1. 开票单位下拉框加入到查询条件右边，并更改为：开票单位查询:
2. 对于发票到款且不为红字发票的记录，将设为红字发票的名称，改为作废，红色显示，并添加撤销到款的按钮
3. 所有记录都应当支持编辑和删除按钮

6. 实现发票添加功能，点击后创建发票添加弹窗，填入必要的字段信息，其中开票日期默认今天，开票单位自动填入下拉框的公司名称，到账日期和到账金额记录不能在里面填入，发票性质是下拉框


发票查询统计中优化：
1. 在冲红后，将发票状态设置为已冲红，操作中就不要展示冲红按钮了，新建的红字发票设置为红字发票
将该记录设置为已冲红状态，并自动开具发票状态为红字发票的发票，金额自动填充为原先的金额的负数，开票日期自动为开票日期
2. 为了便于管理，对于红字发票提供一个查看原发票号码的操作
3. 将【作废】名称修改为冲红，点击后不是一个确认弹窗了，而是新建一个模态框，标题为：新建红字发票（冲红开票{发票号码}），里面的选项可以参考创建发票的内容，但开票金额要自动填充为原先金额的负数，不可编辑，并且在第一行加入原发票号码的字段，显示原先发票号码，不可编辑
2. 去除背景颜色的展示，自定义列中默认展示发票状态
3. 根据修改后的发票规则，重新设计符合冲红逻辑的发票数据mock

首页：
1. 首页不要用警告插件了，直接展示即可

首页不要用警告插件了，直接展示即可，

登录页面密码下面添加记住密码，默认勾选，选上时会自动填充密码

1. 优化公司管理、客户管理的筛选样式，然后在公司管理的创建公司左边添加导出公司，在客户管理的创建客户前面添加导出客户
2. 实现导出公司、客户、发票管理的功能，注意备注部分要分成三个列：备注1、备注2、备注3

