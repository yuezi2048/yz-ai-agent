**二、基础数据**（存在问题）

**（1）公司信息**

①查询条件

公司名称（用复选模态框）     公司税号        法人

②自定义列默认：序号、公司名称、公司税号、法人、注册电话、银行名称、银行账号、注册地址、联系人、联系电话（默认状态不变化，不含备注、操作）

③查询结果-字段：序号、公司名称、公司税号、法人、注册电话、银行名称、银行账号、注册地址、联系人、联系电话。

固定 序号、公司名称、公司税号、法人、注册电话、银行名称、银行账号等列，其他列设定好列宽。

④点字段名称需要能排序

⑤**添加-公司信息**（需连续添加多条记录）

*公司名称        公司税务

法人        注册电话

注册地址  

银行名称        银行账号

联系人        联系电话

备注 



**（2）发票种类（原来的发票类型改成该名称）**

**①查询条件**

发票种类（用复选模态框，要求查询所有的发票）     启用状态（用复选模态框，启动或者禁用两项）

②自定义列默认：序号、发票种类、排列顺序、启用状态、备注（默认状态不变化，不含操作）

③查询结果-字段：序号、发票种类、排列顺序、启用状态、备注

固定 序号、发票种类，排列顺序3列，不随横向滚动条移动。

添加-删除操作

④点字段名称需要能排序

⑤**添加-发票种类**（添加标题为该，修改则为对应的，后续功能模块同理）

*发票种类

排列顺序

启用状态

备注

 

**（3）票据种类（原来的转账方式改成该名称）**

**①查询条件**

票据种类（用复选模态框，要求查询所有票据种类）     启用状态（用复选模态框）

②自定义列默认：序号、票据种类、排列顺序、启用状态、备注（默认状态不变化，不含操作）

③查询结果-字段：序号、票据种类、排列顺序、启用状态、备注

固定 序号、票据种类，排列顺序3列，不随横向滚动条移动。

添加-删除操作

④点字段名称需要能排序

⑤标题：**添加/修改-发票类型**

*票据种类

排列顺序

启用状态

备注

​      

**（4）销票标注（原来的标记改成该名称）**

**①查询条件**

标注（用复选模态框，要求查询所有标注）     启用状态（用复选模态框）

②自定义列默认：序号、标注、排列顺序、启用状态、备注（默认状态不变化，不含操作）

③查询结果-字段：序号、标注、排列顺序、启用状态、备注

固定 序号、标注，排列顺序3列，不随横向滚动条移动。

④点字段名称需要能排序

⑤标题：**添加/修改-销票标注**

*票据种类

排列顺序

启用状态

备注

 

以下是未实现的页面，需要在src/page/base下新建page，然后参考其他page的抽象方式，需要抽取函数到hooks，使用公共组件/模态框，样式定义完成

**（5）进票用途（采购付款、费用报销、其他用途）**

**①查询条件**

**进票用途**（用复选模态框，要求查询进票用途，目前还没有实现进票用途表接口，请在src/mock中先定义）     启用状态（用复选模态框）

②自定义列默认：序号、标注、排列顺序、启用状态、备注（默认状态不变化，不含操作）

③查询结果-字段：序号、标注、排列顺序、启用状态、备注

固定 序号、标注，排列顺序3列，不随横向滚动条移动。

④点字段名称需要能排序

⑤标题：**添加/修改-标注**

*票据种类

排列顺序

启用状态

备注

 

**（6）****权限管理（财务岗位、业务岗位、其他用途）**

权限说明：

**财务岗位：访问财务管理、员工信息模块、修改自己个人信息及登录密码。**

**业务岗位：访问客户管理模块、发票管理模块、供应商模块、基础信息模块**

**管理员：访问所有权限**

 

**①查询条件**

人员权限（用复选模态框，要求查询所有权限，目前还没有实现人员权限表，请在src/mock中先定义）     启用状态（用复选模态框）

②自定义列默认：序号、人员权限、排列顺序、启用状态、备注（默认状态不变化，不含操作）

③查询结果-字段：序号、人员权限、排列顺序、启用状态、备注

固定 序号、标注，排列顺序3列，不随横向滚动条移动。

④点字段名称需要能排序

⑤标题：**添加/修改-标注**

*人员权限

 排列顺序

启用状态

备注





首先说明：如果筛选条件为基本信息（公司信息、发票种类、启动状态、票据种类、标注、发票用途、人员权限）的，一律使用 多选模态框，如果是输入框为基本信息的，一律使用自动填充框（自动查找所有列表，并支持输入筛选），同时模态框内的 备注则需要一个即可，对应的是remark1。模态框的样式务必要保证和之前的一样（即每行的第一个标题框要和上方标题框右对齐，每行最后一个输入框要和上方输入框对齐）

现在请你对以下三个功能模块进行调整优化：

**三、客户信息管理**

①查询条件：

客户单位       客户姓名        客户电话   

公司税号         法人        注册地址         



②自定义列按统一样式

默认序号、客户单位、公司税号、客户姓名（原联系人）、客户电话、银行名称、银行账号、法人、注册电话、注册电话、邮箱、微信。

④查询结果-默认显示顺序：序号、客户单位、公司税号、客户姓名、客户电话、银行名称、银行账号、法人、注册电话、注册地址、邮箱、微信、经营范围、业务经理、添加日期（系统自动）、备注1。（前7列不随左右拖动条隐藏）

（默认一行显示 序号4个字符位；客户单位、公司税号等20个字符位；联系人等8个字符位，联系电话13个字符位）。

**⑤添加客户页（同编辑页） 要批量录入。** 标题也是同格式：添加/修改-客户，后面同理就不再赘述

*客户单位    客户姓名    客户电话

公司税号      法人    注册电话 

注册地址      邮箱      微信     

银行名称    银行账号    业务经理（公司内部）

经营范围

备注



**四、供应商信息**

①查询条件：

供货单位       供货姓名         法人

公司税号       注册地址       经营范围



②自定义列按统一样式

序号、供货单位、公司税号、供货姓名、供货电话、银行名称、银行账号、法人、注册电话、注册地址、邮箱、经营范围。（前7列不随左右拖动条隐藏）

③创建供应商改为 添加供应商，统一样式

③查询结果-默认显示顺序：序号、供货单位、公司税号、供货姓名、供货电话、银行名称、银行账号、法人、注册地址、注册电话、邮箱、经营范围、添加日期（系统自动）、备注1。

（默认一行显示 序号4个字符位；供货单位、公司税号等20个字符位；联系人等8个字符位，联系电话11个字符位）。

 

**④添加供应商页（编辑供应商同）**

*公司名称    供货姓名     供货电话

公司税号       法人     注册电话     

注册地址       邮箱       微信      

银行名称     银行账号                     

经营范围

备注

 

**五、销项发票（发票查询统计修改为该名称）**

**（1）查询页的显示样式要求**

①发票查询条件（字段名称右对齐）

公司名称     业务经理（原业务员） 客户单位      客户姓名   开票人 

开票日期     开票金额     发票号码      发票种类     标注（原标识）



注：开票日期、公司名称、客户单位、发票号码字段内容较多，文本框适度长点。业务经理、客户姓名、开票人等人名、开票金额、发票种类、标准字段内容少，文本框适度短点。



③自定义列

默认：序号、开票日期、开票金额、发票号码、客户单位、客户姓名、发票种类、标注（默认不勾选）、到款日期（最后一次入账）、到款金额（最后一次入账累计金额）、业务经理、公司名称、开票人。

④查询结果-显示顺序（前8列不隐藏）

默认：序号、开票日期、开票金额、发票号码、客户单位、客户姓名、发票种类、标注（默认不显示）、到款日期（最后一次入账）、到款金额（最后一次入账累计金额）、业务经理、公司名称、开票人。

 

**（2）**添加/修改-销项发票

③问题：录入金额支持负数

⑥需要调用api接口判断（传入客户单位+客户姓名请求existsClientUsingPost接口，如果存在，则在客户单位和客户姓名下方描述：已成功关联客户信息：客户单位-客户姓名(clientId[0])，如果不存在，则在下方描述：客户信息关联失败，请[点此链接]创建，点击后提示是否要创建客户单位-客户姓名的客户信息？添加重构后，则下方提示成功关联的提示，请注意，下方的可搜可输的意思是自动填充框，需要搜索所有的列表，并支持输入筛选列表的功能），业务经理（原业务员）、开票人也是同理（existsEmployeeUsingPost）

*开票日期（易填也可选）         *开票金额       

*发票号码               *客户单位（可搜可输）

 客户姓名**+**              发票种类（同公司名称）      

*公司名称（可搜可输）****         *业务经理（可搜可输）  

开票人（可搜可输）****            销项标识（同公司名称） 

备注

  